/**
 * Plugin constants (hardcoded configuration) for frontend code.
 */
export default class TeqFw_I18n_Front_Defaults {

    DI_I18N = 'i18n'; // DI key to set/get 'i18next' lib to/from container

    constructor() {
        Object.freeze(this);
    }
}
